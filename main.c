#include "monty.h"

/**
 * main - interpreter of mony language.
 * @argc: argument count.
 * @argv: argument count.
 * Return: value 0 at success
 */

int main(int argc, char **argv)
{
	(void) argv;

	check_arg(argc);

	return (0);
}
